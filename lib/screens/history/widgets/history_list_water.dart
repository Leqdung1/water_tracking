import 'package:flutter/material.dart';

class HistoryListWater extends StatelessWidget {
  const HistoryListWater({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}